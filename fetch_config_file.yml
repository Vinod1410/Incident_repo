---
- name: Read users from CSV file and return a dictionary
  hosts: localhost
  tasks:
    - name: Load data from CSV file
      set_fact:
        users_data: "{{ lookup('csvfile', 'https://github.com/Vinod1410/Incident_repo/blob/main/server_data.csv') }}"
    - debug:
        var: users_data



        
    
      

    # Optionally, you can add more tasks here to process or deploy the fetched config file
